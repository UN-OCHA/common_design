{#
/**
 * @file
 * Theme override for the taxonomy term form content.
 *
 * This template will be used when a taxonomy term edit form specifies
 * 'taxonomy_term_edit_form' as its #theme callback.
 *
 * Available variables:
 * - form: The taxonomy term add/edit form.
 *
 * @see common_design_form_taxonomy_term_form_alter()
 */
#}

{{ attach_library('common_design/cd-form') }}

{{ form }}
